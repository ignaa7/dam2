import { DirectivaAParametrizadaDirective } from './directiva-a-parametrizada.directive';

describe('DirectivaAParametrizadaDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaAParametrizadaDirective();
    expect(directive).toBeTruthy();
  });
});
