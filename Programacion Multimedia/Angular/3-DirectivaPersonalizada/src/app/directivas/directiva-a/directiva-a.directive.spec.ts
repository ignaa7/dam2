import { DirectivaADirective } from './directiva-a.directive';

describe('DirectivaADirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaADirective();
    expect(directive).toBeTruthy();
  });
});
